# 会话总结（2026-03-24）

## 用户约定（重要）

- 用户声明：**以后说「备份」聊天 = 备份到 GitHub 仓库 [weic23614-coder/yiyaowang](https://github.com/weic23614-coder/yiyaowang) 的 [`对话记录/`](https://github.com/weic23614-coder/yiyaowang/tree/main/%E5%AF%B9%E8%AF%9D%E8%AE%B0%E5%BD%95) 目录**，并同步 `git push`。

## 本期目标与结果

1. **组货「点了没反应」**：默认改为规则引擎优先（可选 AI）、延长超时、sessionStorage 恢复 batch、规则关联放宽兜底、空 batch 返回明确 400。
2. **库存清单**：表格最左增加 **序号**（按分页连续编号）；展示字段做 HTML 转义。
3. **组货方案**：增加 **组货卖点（C 端一句话）** 列，数据来自 `sales_copy`，带 **复制卖点** 按钮。
4. **话术风格**：规则引擎与百炼提示对齐「用药组合」式——主品功效 + 搭配品功效 + 场景收口；去掉干巴巴后台腔。
5. **运维指导**：解释「重启 uvicorn」、`export` 行不要跟中文、`address already in use` 时先 `lsof`/`kill` 再启动。

## 续接说明

代码变更均在 **`zhinengzuhuo-backup`**；重新生成组货后 `sales_copy` 才会批量更新为新话术。
