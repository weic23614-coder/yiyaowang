# 代码与界面变更（zhinengzuhuo-backup）

## 文件清单

| 文件 | 变更摘要 |
|------|-----------|
| `app/templates/admin.html` | 库存序号列；组货卖点列 + 复制；`opsUseAi` 默认不勾；`persistOpsBatch`/`restoreOpsBatchFromStorage`；按钮「生成组货方案」；`escapeHtml`、`copySellingPointPrev` |
| `app/bundle_engine.py` | `build_consumer_sales_copy`、`_short_display_name`、`_symptom_snippet_for_product`；关联门槛无命中时放宽仍保医学安全 |
| `app/ai_brain.py` | 系统提示与 `style_example`：用药组合页风格 `sales_copy` |
| `app/main.py` | `generate-strategies` 无 mains 时 400；AI 空 `sales_copy` 时 `BundleEngine.build_consumer_sales_copy` 兜底 |

## 用户操作提示

- 后台地址：`http://127.0.0.1:<端口>/admin`（与 uvicorn 端口一致）。
- 看新话术：重启服务后 **重新点「生成组货方案」**（旧库内记录不会自动改写）。
