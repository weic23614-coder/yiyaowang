# 明日续工清单

## 1. 环境清理

- [ ] 结束所有残留 **Python / uvicorn**，避免多进程共写同一 `app.db`。
- [ ] 明确 **一个服务进程 + 一个 DB 文件**（推荐长期用 `APP_DB_PATH=.../app_runtime.db`）。

## 2. 功能复现（粘贴聊天记录前可先跑通）

- [ ] 启动单实例，打开 `/admin`。
- [ ] 上传小表或真实表 → 确认返回 `batch_id`、无长时间卡住。
- [ ] 点击 **AI 组货 / 生成策略** → 等待完成或看到明确超时/错误。
- [ ] 查库：`ai_usage_logs` 是否有新行；管理端「来源」是否显示百炼/模型调用。

## 3. 若仍「无反应 / 费用不变」

- [ ] 浏览器 **Network** 看 `generate-strategies`（或等价接口）是否 pending、状态码、响应体。
- [ ] 服务端日志是否有异常栈或超时。
- [ ] 确认 **预算页** API Key / Base URL / 超时已保存且对应当前 DB。

## 4. Git

- [ ] 确认 `zhinengzuhuo-backup` 最新改动是否已 push 到 `zhinengzuhuo`。
- [ ] 确认本仓 `yiyaowang` 已 `git pull` / `push`，本存档目录在远端可见。

## 5. 安全

- [ ] 若 Key 曾泄露，在百炼控制台 **轮换**。
- [ ] `.env`、密钥勿提交。

## 6. 继续对话时建议附带

- 当前使用的 **端口号**、`APP_DB_PATH` 值。
- 一次失败的 **Network 截图或 curl 命令与响应**。
- 相关 **日志片段**（脱敏后）。
