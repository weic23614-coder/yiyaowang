# 会话总结（2026-03-23）

## 目标

- 本地可跑的 **AI 智能组货 MVP**：后台 + 组货、库存、预算、**百炼（通义千问）** 真调用。
- 运营侧：**库存表头固定顺序**、列表与组货结果 **每页 50 条**。
- 业务：**只管组货、不管定价策略**；换购副品展示用 **`original_price`（商品原价）**；侧重 **关联度 + 医学安全**；**防单品灌场**；弱关联过滤。
- 数据：待组货 Excel **可无独立「价格」列**（可用 GMV/销量推导或占位），**不得因缺价拦截上传**。

## 主要结论

1. **SQLite `database is locked`**：已通过 `busy_timeout`、连接 `timeout`、WAL、启动 pragma 重试、写库重试等缓解；根因常为 **多进程 uvicorn 共写同一 `app.db`**——应 **单进程 + 独立库**（如 `APP_DB_PATH=.../app_runtime.db`）。
2. **上传/生成卡死**：已做流式读行、候选池预筛与上限、前端上传/生成超时与错误提示。
3. **「点了 AI 组货没反应、费用不变」**：可能为请求未完成、多实例抢库、或未走记 Token 路径；需在 **固定端口 + 单一 DB** 上完整复现并查 `ai_usage_logs` / 管理端来源。
4. **密钥**：曾在对话/界面出现 Key 形态字符串，建议 **轮换**，勿进 Git。

## 当前状态（会话结束时）

- 工程目录：`/Users/weipeng/zhinengzuhuo-backup`
- 曾用独立库：`APP_DB_PATH=/Users/weipeng/zhinengzuhuo-backup/app_runtime.db`，并从旧 `app.db` 拷贝过 `llm_settings`、`products` 等以便 AI 可用。
- 端口历史上混用 **8088～8098**；建议次日统一为 **单实例 + 明确端口**（例如 8098，以用户实际为准）。
