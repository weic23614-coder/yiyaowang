# yiyaowang 文档仓库（指针）

**主工作目录已迁至独立资料夹（避免与其它项目混在一起）：**

👉 **`/Users/weipeng/药网组件化代运营`**

该目录内含：根目录 **B2C 代运营（01～04）**，以及子目录 **`组件化/`**（**05**、**按日期的会话记录**、**备份约定**）。详见其 `README.md`。

---

本仓库（`~/yiyaowang`）结构建议：**`B2C代运营/`**（代运营主线）、**`组件化/`**（组件化 + OpenClaw + 小程序 Demo）、[**`智能组货/`**](智能组货/)（**1药网 AI 智能组货 MVP 的 PRD 与产品说明**）、[**`对话记录/`**](对话记录/)（**全仓 Cursor 聊天备份与约定，防上下文丢失**）。**日常编辑**可与 **`~/药网组件化代运营`** 对齐；若仍存在 **`其他项目/`**，可视作历史备份，新内容请归入 `组件化/`、`智能组货/` 或 `对话记录/`。

## GitHub

仓库地址：<https://github.com/weic23614-coder/yiyaowang>

推送前请在终端进入 **`药网组件化代运营`** 或按需同步，避免与 `chat-backup` 等仓库混用。
